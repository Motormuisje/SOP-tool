[pytest]
markers =
    no_fixture: tests that do not require SOP_GOLDEN_FIXTURE or PlanningEngine
    browser: Playwright/browser validation tests that require a local app server
    golden: tests that require SOP_GOLDEN_FIXTURE and the golden baseline
    performance: large or slower guardrail tests run outside the quick suite
norecursedirs = compare
